/**
 * Phase 6 demo target — public, well-known, fits metadata size limits (~5–20k LOC class libraries).
 * Override with env DEMO_REPO_URL when running prewarm if you switch repos.
 */
export const DEMO_TARGET_REPO_URL =
  process.env.DEMO_REPO_URL ?? "https://github.com/colinhacks/zod";
